# Repository listing

Use one of these descriptions in the GitHub repository's **About** section.

**English**

> A lightweight macOS menu bar app for finding and safely cleaning developer caches and temporary build files.

**Português**

> Um app leve para a barra de menus do macOS que encontra caches e arquivos temporários de desenvolvimento para liberar espaço com segurança.

Suggested GitHub topics: `macos`, `swift`, `swiftui`, `menu-bar-app`, `developer-tools`, `disk-cleanup`, `cache-cleaner`, `xcode`, `gradle`, `vibe-coding`.
